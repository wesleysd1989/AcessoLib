{
  "name": "acesso-bank-lib",
  "version": "0.0.1",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "android": "react-native run-android",
    "ios": "react-native run-ios",
    "start": "react-native start",
    "start:clean": "react-native start --reset-cache",
    "test": "jest",
    "lint": "eslint .",
    "clean": "yarn clean:android && yarn clean:ios && rm -rf node_modules",
    "clean:android": "rm -rf android/app/build && rm -rf android/build && rm -rf android/.gradle && rm -rf android/build.gradle-e",
    "clean:ios": "rm -rf ios/build && rm -rf ios/Pods && rm -rf ios/AcessoBankLib.xcworkspace"
  },
  "dependencies": {
    "acesso-lib": "https://github.com/wesleysd1989/AcessoLib.git",
    "react": "16.13.1",
    "react-native": "0.63.3",
    "styled-components": "^5.2.1"
  },
  "devDependencies": {
    "@babel/core": "^7.8.4",
    "@babel/runtime": "^7.8.4",
    "@react-native-community/eslint-config": "^1.1.0",
    "babel-jest": "^25.1.0",
    "eslint": "^6.5.1",
    "jest": "^25.1.0",
    "metro-react-native-babel-preset": "^0.59.0",
    "react-test-renderer": "16.13.1"
  },
  "jest": {
    "preset": "react-native"
  }
}
